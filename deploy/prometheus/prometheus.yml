global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: carrier-gateway
    static_configs:
      - targets: ["gateway:8080"]
    metrics_path: /metrics
